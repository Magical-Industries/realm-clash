{
  "name": "@magicalindustries/realm-clash-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:core && vite",
    "build": "npm run build:core && vite build",
    "preview": "vite preview",
    "build:core": "npm run build --prefix ../core"
  },
  "dependencies": {
    "@magicalindustries/realm-clash-core": "file:../core",
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}